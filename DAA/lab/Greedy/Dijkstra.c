#import <stdio.h>
#import <stdbool.h>

int Shortestpath(int v,float cost, int dist[], int n){
    bool s[n]
    dist[v] = 0;
    for (int i=1;i<n;i++) {
        s[i] = false;
        dist[i]=cost(v,i);

        for (int i=2;i<=n-1;i++) {
            s[u] = true;
            for (int i=2;i<n;i++) {
                s[u]=true;
            }
            
        }
    }
}